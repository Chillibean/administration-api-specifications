{
  "description": "Subject requirements for an account",
  "type": "object",
  "properties": {
    "include_site_prefix": {
      "type": "boolean",
      "description": "Include site prefix",
      "example": true
    },
    "characters": {
      "type": "array",
      "description": "characters",
      "items": {
        "type": "string",
        "example": "ABC"
      }
    }
  },
  "required": ["include_site_prefix", "characters"],
  "additionalProperties": false
}
